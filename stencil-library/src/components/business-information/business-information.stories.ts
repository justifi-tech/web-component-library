export default {
  title: 'Components/BusinessInformation',
  component: 'justifi-business-information',
  parameters: {},
};

type Args = {
}

const Template = (args: Args) => {
  return (`
      <justifi-business-information />
  `);
};

export const Basic = Template.bind({});
Basic.args = {
}
