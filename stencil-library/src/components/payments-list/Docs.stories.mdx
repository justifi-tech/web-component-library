import { Meta, Title, Subtitle, Description, ArgTypes, Source } from '@storybook/blocks';
import { filterDocsByTag, ExportedParts } from '../../../storybook-pages/utils.tsx';
import example from './example.js';

<Meta title="Components/PaymentsList" Components="justifi-payments-list" />

<Title />
Component to render a formated list of payments for the requested account.

# Index

- [Props, events and methods](#props-events-and-methods)
- [Styling](#styling)
  - [Exported Parts](#exported-parts)
- [Example usage](#example-usage)

# Props, events and methods

---

<ArgTypes exclude={['css-variables']} />

# Styling

---

The `<justifi-payments-list>` can be customized both by overriding `CSS variables` or by using the exported `parts`.

#### The full list of variables that can be overriden is listed in the [Introduction](/docs/introduction--docs#styling-components-with-variables).

## Exported parts

<ExportedParts
  compact
  component="justifi-payments-list"
  tags={filterDocsByTag('justifi-payments-list').docsTags.map(part => part.text)}
/>

# Example Usage

---

<Source dark language="html" code={example} />
