import { Meta, Title, Subtitle, Description, ArgTypes, Source } from '@storybook/blocks';
import { filterDocsByTag, ExportedParts } from '../utils';
import codeExample from './payouts-list-example';

<Meta
  title="Components/PayoutsList"
  Components="justifi-payouts-list"
/>

<Title />
Component to render a formated list of payouts for the requested account.

# Index

- [Props, events and methods](#props-events-and-methods)
- [Styling](#styling)
  - [Exported Parts](#exported-parts)
- [Example usage](#example-usage)

# Props, events and methods

---

<ArgTypes exclude={['css-variables']} />

# Styling

---

The `<justifi-payouts-list>` can be customized by using the exported `parts`.

## Exported parts

<ExportedParts
  compact
  component="justifi-payouts-list"
  tags={filterDocsByTag('justifi-payouts-list').docsTags.map(part => part.text)}
/>

# Example Usage

---

<Source dark language="html" code={codeExample} />
