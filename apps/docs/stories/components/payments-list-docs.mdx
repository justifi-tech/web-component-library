import {
  Meta,
  Title,
  Subtitle,
  Description,
  ArgTypes,
  Source,
} from '@storybook/blocks';
import { filterDocsByTag, ExportedParts } from '../utils';
import {
  codeExampleFull,
  codeExampleEventHandling,
} from './payments-list-example';
import { Authorization } from '../utils/documentation-components';

<Meta title="Payment Facilitation/Merchant Tools/Payments List" Components="justifi-payments-list" />

<Title />

Component to render a formated list of payments for the requested account.

# Index

* [Props, events and methods](#props-events-and-methods)
  * [Events usage](#events-usage)
* [Styling](#styling)
  * [Exported Parts](#exported-parts)
* [Example usage](#example-usage)

# Props, events and methods

***

<ArgTypes exclude={['theme']} />

<Authorization actions={['read', 'write']} entity="account" identification="account_id" />

## Events usage

All table components emit an event when a row is clicked with the necessary information to call the appropriate `Details` component for that entity.

<Source dark language="html" code={codeExampleEventHandling} />

# Styling

***

The `<justifi-payments-list>` can be customized by using the exported `parts`.

## Exported parts

<ExportedParts
  compact
  component="justifi-payments-list"
  tags={filterDocsByTag('justifi-payments-list').docsTags.map(
    (part) => part.text
  )}
/>

# Example Usage

***

<Source dark language="html" code={codeExampleFull} />
