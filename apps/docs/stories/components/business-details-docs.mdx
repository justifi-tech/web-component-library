import { Meta, Title, ArgTypes, Source } from '@storybook/blocks';
import { filterDocsByTag, ExportedParts } from '../utils';
import { codeExampleFull } from './business-details-example';
import { Authorization } from '../utils/documentation-components';
import * as JustifiBusinessDetails from './business-details.stories.tsx';

<Meta
  title="Entities/BusinessDetails"
  Components="justifi-business-details"
  of={JustifiBusinessDetails}
/>

<Title />

Component to render detailed information about a business. You will need to first
create a business via [Business API](https://docs.justifi.tech/api-spec#tag/Business) to get
the business-id required for this component.

# Index

- [Props and methods](#props-and-methods)
- [Styling](#styling)
  - [Exported Parts](#exported-parts)
- [Example usage](#example-usage)

# Props and methods

---

<ArgTypes exclude={['css-variables']} />

<Authorization
  actions={['read']}
  entity="business"
  identification="business_id"
/>

# Styling

---

The `<justifi-business-details>` can be customized by using the exported `parts`.

## Exported parts

<ExportedParts
  compact
  component="justifi-business-details"
  tags={filterDocsByTag('justifi-business-details').docsTags.map(
    (part) => part.text
  )}
/>

# Example Usage

---

<Source dark language="html" code={codeExampleFull} />
