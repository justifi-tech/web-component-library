import {
  Meta,
  Title,
  Subtitle,
  Description,
  ArgTypes,
  Source,
} from '@storybook/blocks';
import { filterDocsByTag, ExportedParts } from '../utils';
import example from './business-form-example';
import { Authorization } from '../utils/documentation-components';
import * as JustifiBusinessForm from './business-form.stories.tsx';

<Meta
  title="Entities/BusinessForm"
  Components="justifi-business-form"
  of={JustifiBusinessForm}
/>

<Title />

Component to render a business onboarding form. You will need to first create a business
via [Business API](https://docs.justifi.tech/api-spec#tag/Business) to get the business-id
required for this component.

# Index

- [Props, events and methods](#props-events-and-methods)
- [Example usage](#example-usage)

# Props, events and methods

---

<ArgTypes exclude={['css-variables']} />

<Authorization
  actions={['write']}
  entity="business"
  identification="business_id"
/>

# Styling

---

The `<justifi-business-form>` can be customized by using the exported `parts`.

## Exported parts

<ExportedParts
  compact
  component="justifi-business-form"
  tags={filterDocsByTag('justifi-business-form').docsTags.map(
    (part) => part.text
  )}
/>

# Example Usage

---

<Source dark language="html" code={example} />
