---
title: Sandbox Import
---

import TokenizeDoc from '@justifi/webcomponents/docs/components/tokenize-payment-method.mdx';
import { docsSidebar } from '@justifi/webcomponents/docs/sidebar';

# Sandbox Import

Below is the upstream `<justifi-tokenize-payment-method>` documentation rendered directly from `@justifi/webcomponents`.

<TokenizeDoc />

<details>
  <summary>Sidebar data preview</summary>

  <pre>
    {JSON.stringify(docsSidebar.docs.slice(0, 1), null, 2)}
  </pre>
</details>

