---
id: payout-transactions-list
title: Payout Transactions List
description: Ledger of transactions contributing to a payout, with filtering and export support.
sidebar_position: 42
---

import {
  PropsMarkdownTable,
  PartsMarkdownTable,
  getPropsFromDocs,
  getComponentParts,
  getWebcomponentsVersion,
  ComponentBox,
  setUpMocks,
} from '../helpers';
import docsJson from '../docs.json';
import componentTreeJson from '../component-tree.json';
import '@justifi/webcomponents/dist/module/justifi-payout-transactions-list';

{setUpMocks()}

## Overview

Component to display a list of payout transactions for a specific payout.

## Usage

<pre>
  <code className="language-html">{`<justifi-payout-transactions-list
  account-id="acc_123"
  auth-token="wct_reporting"
  payout-id="po_456"
/>`}</code>
</pre>

## Props, Events & Methods

<PropsMarkdownTable
  props={getPropsFromDocs('justifi-payout-transactions-list', docsJson)}
/>

### Events

- `click-event`: row selection event that surfaces `transaction_id`.

### Public methods

1. `refresh()` – Reloads the ledger.

## Theming & Layout

<PartsMarkdownTable
  parts={getComponentParts(
    'justifi-payout-transactions-list',
    componentTreeJson
  )}
/>

# Usage Example

---

<ComponentBox>
  <justifi-payout-transactions-list payout-id="123" auth-token="123abc" />
</ComponentBox>

---

<pre><code className="language-html">{`<!DOCTYPE html>
<html dir="ltr" lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0" />
  <title>justifi-payout-transactions-list</title>

<script
  type="module"
  src="https://cdn.jsdelivr.net/npm/@justifi/webcomponents@${getWebcomponentsVersion()}/dist/webcomponents/webcomponents.esm.js"
></script>

<script
  nomodule
  src="https://cdn.jsdelivr.net/npm/@justifi/webcomponents@${getWebcomponentsVersion()}/dist/webcomponents/webcomponents.js"
></script>

  <style>
      ::part(font-family) {
        font-family: georgia;   
      }
        
      ::part(color) {
        color: darkslategray;
      }

      ::part(background-color) {
        background-color: transparent;
      }
    </style>

</head>

<body>
  <justifi-payout-transactions-list payout-id="po_123" auth-token="authToken" />
</body>

<script>
  (function () {
    const payoutTransactionsList = document.querySelector('justifi-payout-transactions-list');

    payoutTransactionsList.addEventListener('click-event', (event) => {
      // here is where you would handle the click events
      console.log('click-event', event.detail);
    });

    payoutTransactionsList.addEventListener('error-event', (event) => {
      // here is where you would handle the error
      console.error('error-event', event.detail);
    });
  })();
</script>

</html>`}</code></pre>
