---
id: terminal-orders-list
title: Terminal Orders List
description: Reporting table of hardware orders with fulfillment status and tracking info.
sidebar_position: 51
---

import {
  PropsMarkdownTable,
  PartsMarkdownTable,
  getPropsFromDocs,
  getComponentParts,
  getWebcomponentsVersion,
  ComponentBox,
  setUpMocks,
} from '../helpers';
import docsJson from '../docs.json';
import componentTreeJson from '../component-tree.json';
import '@justifi/webcomponents/dist/module/justifi-terminal-orders-list';

{setUpMocks()}

## Overview

Component to render a formated list of terminal device orders for the requested account.

## Usage

<pre>
  <code className="language-html">{`<justifi-terminal-orders-list
  account-id="acc_123"
  auth-token="wct_terminals"
/>`}</code>
</pre>

## Props, Events & Methods

<PropsMarkdownTable
  props={getPropsFromDocs('justifi-terminal-orders-list', docsJson)}
/>

### Events

- `click-event`: Surfaces the order ID for navigation.
- `error-event`: Surfaces API or token errors for logging.

## Theming & Layout

<PartsMarkdownTable
  parts={getComponentParts('justifi-terminal-orders-list', componentTreeJson)}
/>

# Usage Example

---

<ComponentBox>
  <justifi-terminal-orders-list account-id="123" auth-token="123abc" />
</ComponentBox>

---

<pre><code className="language-html">{`<!DOCTYPE html>
<html dir="ltr" lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0" />
  <title>justifi-terminal-orders-list</title>

<script
  type="module"
  src="https://cdn.jsdelivr.net/npm/@justifi/webcomponents@${getWebcomponentsVersion()}/dist/webcomponents/webcomponents.esm.js"
></script>

<script
  nomodule
  src="https://cdn.jsdelivr.net/npm/@justifi/webcomponents@${getWebcomponentsVersion()}/dist/webcomponents/webcomponents.js"
></script>

  <style>
      ::part(font-family) {
        font-family: georgia;   
      }
        
      ::part(color) {
        color: darkslategray;
      }

      ::part(background-color) {
        background-color: transparent;
      }
    </style>

</head>

<body>
  <justifi-terminal-orders-list account-id="acc_123" auth-token="authToken" />
</body>

<script>
  (function () {
    const terminalOrdersList = document.querySelector('justifi-terminal-orders-list');

    terminalOrdersList.addEventListener('click-event', (event) => {
      // here is where you would handle the click events
      console.log('click-event', event.detail);
    });

    terminalOrdersList.addEventListener('error-event', (event) => {
      // here is where you would handle the error
      console.error('error-event', event.detail);
    });
  })();
</script>

</html>`}</code></pre>
