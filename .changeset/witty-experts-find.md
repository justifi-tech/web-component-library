---
"@justifi/types": patch
---

initial release of @justifi/types package with shared TypeScript types, interfaces, and entity classes for JustiFi API resources (payments, payouts, checkouts, disputes, terminals, sub-accounts, etc.)
