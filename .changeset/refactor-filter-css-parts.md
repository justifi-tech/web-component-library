---
"@justifi/webcomponents": minor
---

Replace component-specific CSS filter parts with generic `filter-menu` and `filter-param`. Removes `::part()` selectors like `payments-list-filter-menu` and `payment-id-payments-list-filter-param` in favor of `filter-menu` and `filter-param`.
