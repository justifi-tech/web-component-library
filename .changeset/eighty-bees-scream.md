---
"@justifi/webcomponents": minor
---

- Added new prop `hideCardBillingForm` to `justifi-checkout` and `justifi-tokenize-payment-method` components. When this prop is passed to either component, new credit card payment methods will render with an alternate billing form that only renders the `address_postal_code` field. 
