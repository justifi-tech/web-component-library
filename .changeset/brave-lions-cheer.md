---
"@justifi/webcomponents": minor
---

Add `fillBillingForm` method to `justifi-modular-checkout` and `justifi-tokenize-payment-method` to support pre-filling billing form fields via the checkout store
