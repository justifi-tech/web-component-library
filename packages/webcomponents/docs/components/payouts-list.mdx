---
id: components/payouts-list
title: <justifi-payouts-list>
description: Reporting table for outbound payouts including status, destination, and expected arrival.
sidebar_position: 40
---

import { PropsTable, UsageSnippet, PartsTable } from './helpers';

## Overview

`<justifi-payouts-list>` mirrors the Storybook payouts view, helping finance teams audit disbursements. It surfaces status, amount, destination, and arrival timing, plus links to payout details.

## Usage

<UsageSnippet
  title="Payouts table"
  code={`<justifi-payouts-list
  account-id="acc_123"
  auth-token="wct_reporting"
  columns="created_at,amount,destination,status"
/>`}
  language="html"
/>

## Props, Events & Methods

<PropsTable
  rows={[
    {
      name: 'account-id',
      type: 'string',
      required: true,
      description: 'Filters payouts to the merchant or platform account.',
    },
    {
      name: 'auth-token',
      type: 'string',
      required: true,
      description: 'Token with `read:payouts` scope.',
    },
    {
      name: 'columns',
      type: 'string',
      defaultValue: 'created_at,amount,destination,status,expected_arrival',
      description: 'Comma-separated list controlling table columns.',
    },
    {
      name: 'timezone',
      type: 'string',
      defaultValue: 'UTC',
      description: 'Controls formatting for timestamps.',
    },
  ]}
/>

### Events

- `click-event`: Fires when a row is clicked; `detail.payout_id` indicates which payout to drill into.

### Public methods

1. `refresh()` – Reload the payouts query.

## Theming & Layout

- Share CSS variables with other data tables (`--justifi-table-header-bg`, `--justifi-table-border-color`).
- Provide at least 960px of width for the destination + status columns; horizontal scrolling engages on smaller screens.

<PartsTable
  parts={[
    { name: 'table', description: 'Base table styling for payouts.' },
    { name: 'status-pill', description: 'Badge for Pending/Sent/Failed states.' },
    { name: 'toolbar', description: 'Region for filters or export actions.' },
  ]}
/>

## Accessibility

- Includes `<caption>` text summarizing the table purpose.
- Status pills expose descriptive `aria-label` text with both the status and expected arrival.

## Static examples

<UsageSnippet
  title="Navigate to payout details"
  code={`const payouts = document.querySelector('justifi-payouts-list');

payouts.addEventListener('click-event', (event) => {
  if (event.detail.name === 'row') {
    router.push('/payouts/' + event.detail.payout_id);
  }
});`}
  language="js"
/>

