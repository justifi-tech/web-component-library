---
id: entities/businessdetails/index
title: Business Details
sidebar_label: BusinessDetails
description: Collect and review business profile information with inline validation and KYC-friendly formatting.
sidebar_position: 10
---

import { PropsTable, UsageSnippet, PartsTable } from '../../helpers';

## Overview

Component to render detailed information about a business. You will need to first create a business via [Business API](https://docs.justifi.tech/api-spec#tag/Business) to get the business-id required for this component.

## Usage

<UsageSnippet
  title="Business intake step"
  description="Pass the account context plus the business you want to edit."
  code={`<justifi-business-details
  account-id="acc_123"
  auth-token="wct_onboardingToken"
  business-id="biz_456"
  locale="en-US"
/>`}
  language="html"
/>

## Props, Events & Methods

<PropsTable
  rows={[
    {
      name: 'account-id',
      type: 'string',
      required: true,
      description:
        'Owning platform account; used for authorization and data scoping.',
    },
    {
      name: 'auth-token',
      type: 'string',
      required: true,
      description: 'Short-lived token with `write:onboarding` scope.',
    },
    {
      name: 'business-id',
      type: 'string',
      required: true,
      description: 'Existing business to hydrate; omit for brand-new entities.',
    },
    {
      name: 'locale',
      type: 'string',
      defaultValue: 'en-US',
      description:
        'Controls localization of labels, address formatting, and validation hints.',
    },
    {
      name: 'readonly',
      type: 'boolean',
      defaultValue: 'false',
      description: 'Displays the form as read-only for audit surfaces.',
    },
  ]}
/>

### Events

- `submitted`: Fires when all sections validate and emit an updated business payload.
- `error-event`: Emits when API requests fail or validation errors occur; payload includes field-level hints.

### Public methods

1. `submit()` – Programmatically trigger validation and submission.
2. `reset()` – Clear the form back to the most recently loaded business payload.

## Theming & Layout

<PartsTable
  parts={[
    {
      name: 'section-header',
      description:
        'Wraps each grouping (Business, Address, Tax) so you can adjust typography.',
    },
    {
      name: 'field-group',
      description: 'Controls spacing between related inputs.',
    },
    {
      name: 'cta',
      description:
        'Styles the primary continue/save button if you keep it visible.',
    },
  ]}
/>

## Static examples

<UsageSnippet
  title="Read-only review mode"
  description="Render the component in a confirmation screen without editing enabled."
  code={`<justifi-business-details
  account-id="acc_123"
  auth-token="wct_readOnlyToken"
  business-id="biz_456"
  readonly
/>`}
  language="html"
/>
