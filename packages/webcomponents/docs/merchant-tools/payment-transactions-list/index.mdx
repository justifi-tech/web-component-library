---
id: merchant-tools/payment-transactions-list/index
title: Payment Transactions List
description: Transaction-level ledger for payments, grouped by status and funding state.
sidebar_position: 32
---

import {
  PropsTable,
  UsageSnippet,
  PartsTable,
  PaymentTransactionsListExample,
} from '@justifi/webcomponents/docs/helpers';

## Overview

Component to display a list of payment transactions for a specific payment.

## Usage

<UsageSnippet
  title="Transaction history"
  description="Filter by payment or customer when embedding the ledger."
  code={`<justifi-payment-transactions-list
  payment-id="py_1A2B3C4D5E6F7G8H9I0J"
  auth-token="wct_reporting"
/>`}
  language="html"
/>

## Live Example

<PaymentTransactionsListExample />

## Props, Events & Methods

<PropsTable
  rows={[
    {
      name: 'payment-id',
      type: 'string',
      required: true,
      description: 'The payment ID to fetch transactions for.',
    },
    {
      name: 'auth-token',
      type: 'string',
      required: true,
      description: 'Token with `read:payments` scope.',
    },
    {
      name: 'columns',
      type: 'string',
      description: 'Comma-separated list of column keys to display.',
    },
  ]}
/>

### Events

- `click-event`: Fires when a transaction row is clicked.
- `error-event`: Surfaces API or token failures.

### Public methods

None.

## Theming & Layout

<PartsTable
  parts={[
    {
      name: 'font-family',
      description: 'Controls the font family for the component.',
    },
    {
      name: 'color',
      description: 'Controls the text color.',
    },
    {
      name: 'background-color',
      description: 'Controls the background color.',
    },
  ]}
/>
