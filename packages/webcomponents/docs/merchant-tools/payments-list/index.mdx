---
id: merchant-tools/payments-list/index
title: Payments List
description: Paginated table of payments with filtering, export, and quick actions for ops teams.
sidebar_position: 31
---

import { PropsTable, UsageSnippet, PartsTable } from '../../helpers';

## Overview

Component to render a formated list of payments for the requested account.

## Usage

<UsageSnippet
  title="Payments list with filters"
  code={`<div class="payments-report">
  <justifi-filters
    account-id="acc_123"
    auth-token="wct_reporting"
    target="payments"
  ></justifi-filters>
  <justifi-payments-list
    account-id="acc_123"
    auth-token="wct_reporting"
    columns="created_at,customer_name,payment_amount,status"
  ></justifi-payments-list>
</div>`}
  language="html"
/>

## Props, Events & Methods

<PropsTable
  rows={[
    {
      name: 'account-id',
      type: 'string',
      required: true,
      description: 'Scope results to the platform or sub-account.',
    },
    {
      name: 'auth-token',
      type: 'string',
      required: true,
      description: 'Token with `read:payments` scope.',
    },
    {
      name: 'columns',
      type: 'string',
      defaultValue:
        'created_at,customer_name,payment_amount,payment_description,status',
      description:
        'Comma-delimited list that controls which columns render and their order.',
    },
    {
      name: 'page-size',
      type: 'number',
      defaultValue: '25',
      description: 'Override default pagination size.',
    },
    {
      name: 'enable-export',
      type: 'boolean',
      defaultValue: 'false',
      description: 'Adds an export button that streams CSV data when true.',
    },
  ]}
/>

### Events

- `click-event`: emitted when users select a row or click an inline action; `event.detail` surfaces `payment_id`.
- `error-event`: surfaces API or token errors for logging.

### Public methods

1. `refresh()` – Re-run the underlying query (useful after applying filters).
2. `exportCSV()` – Programmatically trigger an export if `enable-export` is true.

## Theming & Layout

- Tweak table colors using CSS properties like `--justifi-table-header-bg`, `--justifi-table-border-color`, and `--justifi-table-row-hover-bg`.
- Keep ample horizontal space (min 960px) for full column labels; on smaller screens the table scrolls horizontally while keeping the first column sticky.

<PartsTable
  parts={[
    {
      name: 'toolbar',
      description: 'Container for search, export, and batch actions.',
    },
    {
      name: 'table',
      description: 'Main table shell; override typography and spacing here.',
    },
    {
      name: 'status-pill',
      description: 'Styling target for status badges (Paid, Pending, Failed).',
    },
  ]}
/>

## Accessibility

- Table markup follows WCAG guidance (`scope="col"` headers, `<caption>` for summary text).
- Keyboard navigation cycles through rows/columns with arrow keys; focus outlines remain visible per CSS variables.
- Status pills set `aria-label` text describing the underlying status color.

## Static examples

<UsageSnippet
  title="Reacting to row clicks"
  code={`const list = document.querySelector('justifi-payments-list');

list.addEventListener('click-event', (event) => {
if (event.detail.name === 'row') {
router.push('/payments/' + event.detail.payment_id);
}
});`}
language="js"
/>
