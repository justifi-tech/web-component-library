---
id: merchant-tools/payment-details/index
title: Payment Details
description: Detailed view for a single payment, including customer data, timeline events, and dispute shortcuts.
sidebar_position: 30
---

import { PropsTable, UsageSnippet, PartsTable } from '../../helpers';

## Overview

Component to display detailed information about a specific payment.

## Usage

<UsageSnippet
  title="Payment drill-down"
  description="Load by passing the payment identifier and the account context."
  code={`<justifi-payment-details
  account-id="acc_123"
  auth-token="wct_reporting"
  payment-id="pay_456"
/>`}
  language="html"
/>

## Props, Events & Methods

<PropsTable
  rows={[
    {
      name: 'account-id',
      type: 'string',
      required: true,
      description: 'Owning platform account for authorization.',
    },
    {
      name: 'auth-token',
      type: 'string',
      required: true,
      description: 'Token with `read:payments` scope.',
    },
    {
      name: 'payment-id',
      type: 'string',
      required: true,
      description: 'Payment to display.',
    },
    {
      name: 'show-actions',
      type: 'boolean',
      defaultValue: 'true',
      description: 'Hide when you only need a read-only audit surface.',
    },
  ]}
/>

### Events

- `refund-requested`: emitted when an operator clicks the refund CTA.
- `dispute-opened`: indicates that the “Open dispute” action was invoked.

### Public methods

1. `refresh()` – Re-fetch the payment to reflect new statuses after an action.

## Theming & Layout

<PartsTable
  parts={[
    {
      name: 'font-family',
      description: 'Controls the font family for the component.',
    },
    {
      name: 'color',
      description: 'Controls the text color.',
    },
    {
      name: 'background-color',
      description: 'Controls the background color.',
    },
  ]}
/>

## Static examples

<UsageSnippet
  title="Hook into refund events"
  code={`const details = document.querySelector('justifi-payment-details');

details.addEventListener('refund-requested', ({ detail }) => {
console.log('Starting refund flow for payment', detail.payment_id);
});`}
language="js"
/>
