# web-component-library History

<!-- Completed work items, newest first. Do not group by spec — just append at the top. -->

- **core-component-merge v0.1 (2026-03-11):** Merge `payouts-list`: combine `justifi-payouts-list` and `payouts-list-core` into single component with `StyledHost`, delete core files, update tests
- **core-component-merge v0.1 (2026-03-11):** Merge `business-details`: combine `justifi-business-details` and `business-details-core` into single component with `StyledHost`, delete core files, update tests
- **core-component-merge v0.1 (2026-03-11):** Merge `payment-details`: combine `justifi-payment-details` and `payment-details-core` into single component with `StyledHost`, delete core files, update tests
- **core-component-merge v0.1 (2026-03-11):** Merge `payout-details`: combine `justifi-payout-details` and `payout-details-core` into single component with `StyledHost`, delete core files, update tests
