name: Run Tests

on:
  pull_request:
    paths:
      - stencil-library/**

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: stencil-library
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'

      - name: NPM Install
        run: npm install

      - name: Run unit tests
        run: npm test

      - name: Run E2E tests
        run: npm test:e2e
